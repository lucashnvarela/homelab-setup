services:
  server:
    container_name: ps2-smb-server
    image: fortu/ps2smb
    restart: unless-stopped
    ports:
      - 446:445
    volumes:
      - ./config:/etc/samba:ro
      - /mnt/smb_ps2roms:/mnt/smb_ps2roms:rw

networks:
  default:
    name: ps2_smb_network